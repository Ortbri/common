'use client';

// import { InputVanish } from '../../components/animated/input-vanish';

function HomeSearch() {
  // const [state, setState] = useState<string>('');

  return (
    <div></div>
    // <InputVanish
    //   placeholders={['hello there!', 'whats up?', 'whats next?']}
    //   onChange={prev => setState(prev)}
    //   onSubmit={() => console.log('hello there')}
    // />
  );
}

export default HomeSearch;
